

<div class="menu">
  <h2>Menu</h2>

  <% if @user.agents.empty? && @user.funerals.empty?%>

    <%= link_to "Let's get started", new_funeral_path %>


  <% elsif !@user.agents.empty? && @user.funerals.empty?%>

  Link to Agent Index/Show and Link to pick up where you left off, which is with funeral preferences

  <% elsif @user.agents.empty? && !@user.funerals.empty?%>

    Link to Funeral Index/Show and Link to pick up where you left off, which is with agents

  <% elsif !@user.agents.empty? && !@user.funerals.empty?%>

  <!-- Overall Show Link -->
    <%= link_to "Summary of Your Preferences", users_summary_path %>
  <% end %>






  <!-- <div class="identity">Logged in as: <%= @user.username %></div>
  <ul>
    <% if !@user.agents.empty? %>
      <li><%= link_to("See Agent Preferences", agents_path) %></li>
    <% else %>
      <li><%= link_to("Specify Agent Preferences", new_agent_path) %></li>
    <% end %>
    <% if !@user.funerals.empty? %>
      <li><%= link_to("See Funeral Preferences", funeral_path(@user)) %></li>
    <% else %>
      <li><%= link_to("Specify Funeral Preferences", new_funeral_path) %></li>
    <% end %>
    <li><%= link_to("See Funeral Preference Statistics", funerals_path) %></li>
    <li><%= link_to("Log Out", access_logout_path) %></li>
  </ul>
</div> -->
