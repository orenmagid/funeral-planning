<h2>Create a new user account</h2>

<%= render partial: "shared/object_errors.erb", locals: { object: @user } %>

<%= render partial: "form.html.erb", locals: { object: @user } %>
<br><br>

<%= link_to("Login", access_login_path) %> <br>
